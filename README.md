# yarn-next-template
Template for NextJs 
- MUI
- axios
- React Query

```
$ yarn install

If using yarn 3:
$ corepack enable
$ yarn set version stable
$ yarn install
$ yarn dlx @yarnpkg/sdks vscode || yarn dlx @yarnpkg/sdks vim
```
